# 加到 host composition / profile 的 cordis.patch.yml（host 平面）
# 注册 `osDeploy` Service（后台 + 任务队列 + 部署引擎 + HTTP 服务器），
# 其 `dsh.client` 客户端半（浏览器「OS 部署」页）由 modules 扫描自动纳入。
- insert:
    - id: os-deploy
      name: '@qinwei/dsh-os-deploy'
