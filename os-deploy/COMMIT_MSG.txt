revert(debian): choose_interface 回 auto，去掉硬编码接口名字段

重新确认：enp125s0f0 硬编码方案不妥（换机器/网卡即失效，正是用户指出的问题），
且根因判断本身有误——回看 2/3 次任务 early-apt-config 均正常到达，说明 auto
在"选网卡"这步已成功（d-i 自选首个有链路网卡、网络通、preseed 拉到）。卡顿在
early_command 之后（choose-mirror 不发 fetch），与选网卡无关，故改接口名治不了。

netcfg 的 auto 只读链路标志、不 down/up 端口，"扫描触发 hinic 断网"假设不成立。

- 撤销 nicInterface 字段（DeploySpec/TargetDevice/index/client 全部移除）
- choose_interface 回 auto（动态探测，不写死；2/3 次已证选网卡正常）
- 保留 DEBIAN_FRONTEND=text（通用无头串口设置）：下次重跑 d-i 对话框文本
  按行写到 ttyS0、iBMC SOL 能抓到，据此确诊 early_command 之后卡顿的真实原因
